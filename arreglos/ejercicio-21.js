// Declarar varaible con datos
const nums = [1, 23, 5, 8, 40, 12, 2, 67, 24, 9, 39] 

// Crear ciclo for para imprimir cada valor del array por medio del index
for(let index = 0; index < nums.length; index++){
  // Condicional if para comparar si son mayores a 10 los valores del array
  if(nums[index] > 10){ 
    console.log(nums[index]) //Imprime valores del array 
  }
}
